# 3D 打印耗材烘干控制器

## 控制逻辑

- AHT20 每秒读取一次空气温度和相对湿度。
- BMP280 每秒读取第二路温度，用作独立过温保护。
- 只有系统处于运行状态、启用了加热目标、传感器正常并且风扇状态为 `RUN` 时，才允许 PTC 加热。
- 启用温度阈值时，达到目标值关闭加热，降到目标值以下 1°C 时重新加热。
- 温度阈值设为 `OFF` 时不使用用户温度目标，但仍保留 70°C 安全控温上限和 75°C 锁存过温保护。
- 启用湿度阈值时，达到目标值暂停加热，湿度重新升高到目标值以上 2%RH 时恢复温控。
- 湿度阈值设为 `OFF` 时不按湿度暂停；温度和湿度都设为 `OFF` 时进入只吹风模式，PTC 始终关闭。
- 风扇黄线作为锁转状态输出使用：低电平表示运行，高电平表示停止或断电。
- 运行后先等待风扇建立状态 6 秒；之后高电平持续 500ms 即关闭加热、停止任务并显示 `FAN ERR`。风扇信号重新连续低电平 200ms 后自动清除该故障，但任务和加热不会自动重启。
- AHT20 或 BMP280 检测到 75°C 时，立即关闭加热并锁定过温故障。
- AHT20、BMP280 任一通信失败时，立即关闭加热并锁定传感器故障。
- 传感器离线后每 2 秒复位 I2C2 并重新探测；重新接入后自动恢复数据显示，但已锁存的故障仍需在主页面长按清除。

## 旋转编码器操作

- 空闲主页面短按：依次进入温度设置、湿度设置和启动确认页面。
- 温度页面旋转：选择 `OFF` 或 30~70°C；短按进入湿度页面。
- 湿度页面旋转：选择 `OFF` 或 10~80%RH；短按进入启动确认页面。
- 启动确认页面旋转：选择 `START` 或 `SAVE ONLY`；短按执行所选操作。
- 启用湿度阈值时，以当前湿度是否高于目标判断是否需要工作；仅启用温度阈值时，以当前温度是否低于目标判断。无需工作或传感器离线时，确认页面默认选择 `SAVE ONLY`；需要工作时默认选择 `START`。两项均为 `OFF` 时默认启动只吹风任务。
- 设置过程中长按：返回上一步；温度页面长按取消本次设置并回到主页面。
- 运行中主页面长按 1 秒：停止任务；运行时短按不会进入设置。
- 风扇故障恢复为 `RUN` 后自动清除；传感器和过温故障需先排除原因，再在主页面长按清除。任务均不会自动重启，之后需短按重新设置并在确认页面启动。

默认温度目标为 55°C，默认湿度目标为 20%RH，两项默认启用。识别到 W25Q 系列 SPI Flash 后，目标值及启用状态会在重新上电时自动恢复；旧版保存记录按两项都启用兼容读取。Flash 未识别时继续使用默认值，不影响安全控制。

## STM32 接线

| 设备 | 设备引脚 | STM32F103 引脚 | 说明 |
|---|---|---|---|
| AHT20+BMP280 | VCC | 3.3V | 建议整个 I2C 模块使用 3.3V |
| AHT20+BMP280 | GND | GND | 必须共地 |
| AHT20+BMP280 | SCL | PB10 | I2C2 SCL，独立传感器总线 |
| AHT20+BMP280 | SDA | PB11 | I2C2 SDA，独立传感器总线 |
| SSD1306 | VCC | 3.3V | 128x64 I2C 屏幕，地址 0x3C |
| SSD1306 | GND | GND | 必须共地 |
| SSD1306 | SCL | PB6 | I2C1 SCL，屏幕专用总线 |
| SSD1306 | SDA | PB7 | I2C1 SDA，屏幕专用总线 |
| 旋转编码器 | CLK/S1 | PB12 | 内部上拉 |
| 旋转编码器 | DT/S2 | PB0 | 内部上拉 |
| 旋转编码器 | SW | PB1 | 按下接地，内部上拉 |
| 旋转编码器 | + | 3.3V | 不要接 5V |
| 旋转编码器 | GND | GND | 必须共地 |
| XY-MOS | IN/SIG | PB8 | 高电平有效，TIM4_CH3 |
| XY-MOS | GND | GND | 与 STM32、12V 电源共地 |
| W25Q Flash | CLK/SCK | PA5 | SPI1 时钟 |
| W25Q Flash | DO/MISO | PA6 | Flash 数据输出 |
| W25Q Flash | DI/MOSI | PA7 | Flash 数据输入 |
| W25Q Flash | CS | PB9 | 低电平片选 |
| W25Q Flash | VCC | 3.3V | 不要接 5V |
| W25Q Flash | GND | GND | 必须共地 |
| Nidec 风扇 | 红线 | +12V | 风扇保持全速供电 |
| Nidec 风扇 | 黑线 | GND | 12V 负极并入系统公共地 |
| Nidec 风扇 | 黄线 | PA8 | 开集电极锁转输入，外接 4.7k 上拉到 3.3V；低电平为 RUN |

PB6/PB7和PB10/PB11是两条独立I2C总线，每条总线都需要上拉到3.3V。多数模块已经带上拉电阻；如果对应模块没有，再给该总线的SCL、SDA分别外接约4.7k电阻。不要把I2C上拉到5V。

风扇黄线是锁转状态信号，不提供实际 RPM。程序在 OLED 上显示 `FAN: RUN`、`FAN: STOP` 或启动确认期间的 `FAN: WAIT`。黄线不能直接连接 3.3V 或 12V，必须通过 4.7k 电阻上拉到 3.3V。

如果 12V 风扇侧和 STM32 侧必须保持隔离，黄线不能直接接 PA8。可用 PC817：`12V+ -> 2.2k -> 光耦LED -> 风扇黄线`；光耦晶体管侧 `集电极 -> PA8`、`发射极 -> STM32 GND`，并将 PA8 用 4.7k 上拉到 3.3V。该接法仍保持低电平为 RUN、高电平为 STOP。

Flash 驱动按 3.3V Winbond W25Qxx、SPI 模式 0 实现，启动时读取 JEDEC ID。温湿度目标使用最后两个 4KB 扇区循环保存并带 CRC32 校验，其他 Flash 空间保持未使用，可继续扩展运行数据记录。

## 12V 功率部分

- 12V 电源正负极接 XY-MOS 电源输入端，PTC 接 XY-MOS 负载输出端，具体端子顺序以模块丝印为准。
- 12V 50W PTC 的稳态电流约为 4.2A，电源建议至少 12V/6A；保险丝和线径应按实测冷态启动电流选择，通常可从 5~7.5A 慢熔保险丝范围评估。
- PTC 和风扇的电流不能经过 STM32 开发板供电线。
- STM32 应由 12V 经稳压模块转换为 5V 或 3.3V 供电。
- 所有控制信号必须共地；功率线应使用足够线径，并远离温湿度传感器和信号线。
- AHT20+BMP280 应安装在循环风道中、避开 PTC 直接热辐射，否则测得温度不能代表烘干箱内空气温度。
