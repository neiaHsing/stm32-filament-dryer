# 3D 打印耗材烘干控制器

## 控制逻辑

- AHT20 每秒读取一次空气温度和相对湿度。
- BMP280 每秒读取第二路温度，与 AHT20 按 30% / 70% 融合；融合温度统一用于控制、显示和上报，原始读数保留独立过温保护。
- 只有系统处于运行状态、启用了加热目标、传感器正常并且风扇状态为 `RUN` 时，才允许 PTC 加热。
- 启用温度目标时采用保温前馈、积分修正和预测减功率，每次收到新的有效温度样本时计算一次（约每秒一次），取 0.7×AHT20 + 0.3×BMP280 的融合温度作为反馈。加热由 10 Hz PWM 调节 0–100% 占空比。当前基准参数为 Kp=200、Ki=2、Kd=180、积分分离阈值 2°C（输出单位为千分比），按满占空比实测升温速度调整增益；调节期间电机保持运行。
- 保温前馈以新版 40°C 测试接近目标时的输出为参考，初值约 30% 占空比；其它温度是估计值，由 ±250 千分比的有界积分修正。只在温差不超过 2°C、温度变化率不超过 0.1°C/s 且未处于掉温恢复抑制期时积分；同时考虑输出上下限，避免额外累积。升温速度采用 1 秒时间常数滤波，预测时间随近期输出在 1.7–2.2 秒间变化，不再额外叠加蓄热温升扣减。接近目标且温度下降时，按实测满功率速率估算补偿，额外输出最多 10 个百分点；大幅掉温不叠加此补偿。实测高于目标 0.30°C 时暂停加热。
- 运行中调高目标保留积分修正和温度变化率；调低目标清除正向积分修正。停止、故障或湿度达标时仍复位 PID 状态。异常处理器也直接关闭加热和电机输出。满功率采样见 `outputs/full_power_60c/REPORT.md`，新版策略与 40°C 验证见 `outputs/pid_strategy_40c/REPORT.md`；历史 PID 记录保留在 `outputs/pid_tuning/EXPERIMENTS.md`。
- 目标改变但没有新温度样本时，只重算输出，不推进积分、温度微分或输出历史滤波；新样本之间按传感器触发时间计算 dt，避免独立 PID 定时器与传感器采样错位。
- 板载 PC13 加热指示灯不再简单常亮：按实际加热占空比映射 160 ms～4 s 的闪烁周期，占空比越高越快；占空比为零时立即熄灭。
- 温度阈值设为 `OFF` 时不使用用户温度目标，但仍保留 70°C 安全控温上限和 75°C 锁存过温保护。
- 启用湿度阈值时，达到目标值暂停加热，湿度重新升高到目标值以上 2%RH 时恢复温控。
- 湿度阈值设为 `OFF` 时不按湿度暂停；温度和湿度都设为 `OFF` 时进入只吹风模式，PTC 始终关闭。
- 风扇黄线作为锁转状态输出使用：低电平表示运行，高电平表示停止或断电。
- 运行后先等待风扇建立状态 6 秒；之后高电平持续 500ms 即关闭加热、停止任务并显示 `FAN ERR`。风扇信号重新连续低电平 200ms 后自动清除该故障，但任务和加热不会自动重启。
- AHT20 或 BMP280 检测到 75°C 时，立即关闭加热并锁定过温故障。
- AHT20、BMP280 任一通信失败时，立即关闭加热并锁定传感器故障。
- 传感器离线后每 2 秒复位 I2C2 并重新探测；重新接入后自动恢复数据显示，但已锁存的故障仍需在主页面长按清除。
- 网页和 HTTP API 的远程启动进入相同的安全状态机，不能绕过风扇、传感器、过温或锁存故障检查。
- 由 ESP32 启动的任务采用 5 秒远程租约；ESP32 每秒续租，链路中断或 ESP32 停止发送后 STM32 自动停止任务并关闭 PTC 和电机。

## 旋转编码器操作

- 空闲主页面短按：依次进入温度设置、湿度设置、室温校准和启动确认页面。
- 温度页面旋转：选择 `OFF` 或 30~70°C；短按进入湿度页面。
- 湿度页面旋转：选择 `OFF` 或 10~80%RH；短按进入室温校准页面。
- `ROOM TEMP CAL` 页面旋转：输入当前箱外室温，范围 −10～50°C、每格 1°C，默认 22°C；短按进入启动确认，长按返回湿度页面。−1°C 是有效室温，不表示 OFF。
- 启动确认页面旋转：选择 `START` 或 `SAVE ONLY`；短按执行所选操作。
- 启用湿度阈值时，以当前湿度是否高于目标判断是否需要工作；仅启用温度阈值时，以当前温度是否低于目标判断。无需工作或传感器离线时，确认页面默认选择 `SAVE ONLY`；需要工作时默认选择 `START`。两项均为 `OFF` 时默认启动只吹风任务。
- 设置过程中长按：返回上一步；温度页面长按取消本次设置并回到主页面。
- 运行中主页面长按 1 秒：停止任务；运行时短按不会进入设置。
- 风扇故障恢复为 `RUN` 后自动清除；传感器和过温故障需先排除原因，再在主页面长按清除。任务均不会自动重启，之后需短按重新设置并在确认页面启动。

默认温度目标为 55°C，默认湿度目标为 20%RH，两项默认启用，默认手动室温为 22°C。识别到 W25Q 系列 SPI Flash 后，目标值、启用状态和室温会在重新上电时自动恢复。v1 记录按两项启用、室温 22°C 读取；v2 保留启用状态并补入室温 22°C。v3 仍使用 16 字节记录及原有双扇区/CRC32 机制，字节 11 低两位保存启用状态、高六位保存室温加 10。Flash 不可用时仍可在 RAM 应用设置，断电不保存。

现有前馈曲线的实测环境由用户确认为 22°C。手动室温只补偿保温前馈：`F = clip(F22 × max(目标−室温, 0)/(目标−22), 0, 850)`，其中当前 `F22 = clip(20.13 × (目标−34.65), 0, 850)`，输出单位为千分比。20.13 的斜率由实测点“目标 60°C、室温 25°C 需要 47% 占空比”校准得到，因此该点的前馈为 470‰。34.65 仍是前馈曲线偏移量，不是环境温度。较冷环境增加前馈、较暖环境减少前馈；前馈到达上限后不再增加，总闭环输出仍可达到 100%。此比例是单点校准后的估算，未经过多环境实机标定；满功率速率表不作室温缩放。室温不会改变箱内温度读数或过温保护阈值。用户需在环境变化时重新输入室温。

修改在最终 `START` 或 `SAVE ONLY` 确认后生效，返回主页面取消则不应用。运行中需先停止再修改室温；停止时已有积分会复位。远程控制命令也携带室温字段，ESP32 网页和 HTTP API 可直接读取、修改并显示当前室温前馈参考值；省略该字段的旧版命令按 22°C 兼容处理。

## ESP32 上位机控制

ESP32 网页每秒显示温湿度曲线和 STM32 的实际状态，可执行启动、停止、设置温度目标、设置湿度目标、调节保温前馈室温和清除故障。程序接口为 `GET /api/status` 与 `GET/POST /api/control`，详细 JSON 格式见 [ESP32 网关说明](esp32_gateway/README.md)。

状态和控制命令共用 PB15/GPIO23 一根数据线。STM32 保持 SPI 主机角色：每秒先发送固定 64 字节、带 CRC16 的二进制状态帧，随后释放 PB15 为高阻，约 20 ms 后再次拉低 CS 并产生时钟，接收 ESP32 发出的固定 64 字节命令槽；没有待发命令时，ESP32 返回带 CRC16 的 `N*48FA\r\n`，其余字节补零。PB13 只提供 SCK，PB14 只提供 CS，PB15/GPIO23 按时隙切换方向。PA9、PA10 和 ESP32 GPIO17 均不连接。

经典 ESP32 的 ESP-IDF 没有公开支持三线 SPI 从机的 SIO 模式，本项目通过 GPIO matrix 将 GPIO23 同时路由为从机输入和输出，并只在 64 字节下行时隙打开输出使能。这是配合本项目固定时序使用的 workaround，不应当当作通用三线 SPI 从机接口。PB15 与 GPIO23 建议串联 330 Ω～1 kΩ 电阻限制方向切换异常时的争用电流；现有直连线可以先在 PTC 和电机停止状态下验证通信，再决定是否加串联电阻。

远程启动前，ESP32 要求最近 2.5 秒内收到 STM32 扩展状态；STM32 还会检查命令看到的控制器时间戳是否过期。等待确认期间 ESP32 在每个一秒下行时隙重发同一命令；运行成功后，只有收到新的 STM32 状态才会继续发送租约心跳，任一方向的链路失效都会停止续租。相同会话和序号只应用一次，不会因重发而重复写 Flash。网页提交到 STM32 应用通常需要等待下一个下行时隙，确认再随下一次上行返回，因此界面看到最终确认约有 1～2 秒延迟。

远程设置可以选择 `persist=true` 写入 W25Q Flash。自动化 PID 调整时建议始终使用 `persist=false`，只在确定最终目标需要掉电保存时写入，避免高频调参造成不必要的 Flash 擦写。为避免同步 Flash 操作阻塞安全循环，`persist=true` 只允许与 `running=false` 一起提交；保存成功后需另发启动命令。

## STM32 接线

| 设备 | 设备引脚 | STM32F103 引脚 | 说明 |
|---|---|---|---|
| AHT20+BMP280 | VCC | 3.3V | 建议整个 I2C 模块使用 3.3V |
| AHT20+BMP280 | GND | GND | 必须共地 |
| AHT20+BMP280 | SCL | PB10 | I2C2 SCL，独立传感器总线 |
| AHT20+BMP280 | SDA | PB11 | I2C2 SDA，独立传感器总线 |
| SSD1306 | VCC | 3.3V | 128x64 I2C 屏幕，地址 0x3C |
| SSD1306 | GND | GND | 必须共地 |
| SSD1306 | SCL | PB6 | I2C1 SCL，屏幕专用总线 |
| SSD1306 | SDA | PB7 | I2C1 SDA，屏幕专用总线 |
| 旋转编码器 | CLK/S1 | PB12 | 内部上拉 |
| 旋转编码器 | DT/S2 | PB0 | 内部上拉 |
| 旋转编码器 | SW | PB1 | 按下接地，内部上拉 |
| 旋转编码器 | + | 3.3V | 不要接 5V |
| 旋转编码器 | GND | GND | 必须共地 |
| XY-MOS | IN/SIG | PB8 | 高电平有效，TIM4_CH3 |
| XY-MOS | GND | GND | 与 STM32、12V 电源共地 |
| W25Q Flash | CLK/SCK | PA5 | SPI1 时钟 |
| W25Q Flash | DO/MISO | PA6 | Flash 数据输出 |
| W25Q Flash | DI/MOSI | PA7 | Flash 数据输入 |
| W25Q Flash | CS | PB9 | 低电平片选 |
| W25Q Flash | VCC | 3.3V | 不要接 5V |
| W25Q Flash | GND | GND | 必须共地 |
| Nidec 风扇 | 红线 | +12V | 风扇保持全速供电 |
| Nidec 风扇 | 黑线 | GND | 12V 负极并入系统公共地 |
| Nidec 风扇 | 黄线 | PA8 | 开集电极锁转输入，外接 4.7k 上拉到 3.3V；低电平为 RUN |
| DRV8870 | IN1 | PA0 | TIM2_CH1，20kHz PWM；实际加热时默认 100% 占空比正转 |
| DRV8870 | IN2 | PA2 | 正转时保持低电平，停止时与 IN1 一起为低电平 |
| ESP32-S | SCLK / GPIO18 | PB13 | SPI2 时钟，模式 1，由 STM32 输出 |
| ESP32-S | CS / GPIO27 | PB14 | 低电平片选，由 STM32 控制 |
| ESP32-S | SDIO / GPIO23 | PB15 | 半双工数据：64 B 状态上行、64 B 命令下行 |
| ESP32-S | GND | GND | 两块控制板必须共地 |

PB6/PB7和PB10/PB11是两条独立I2C总线，每条总线都需要上拉到3.3V。多数模块已经带上拉电阻；如果对应模块没有，再给该总线的SCL、SDA分别外接约4.7k电阻。不要把I2C上拉到5V。

风扇黄线是锁转状态信号，不提供实际 RPM。程序在 OLED 上显示 `FAN: RUN`、`FAN: STOP` 或启动确认期间的 `FAN: WAIT`，同一行使用 `M:ON` / `M:OFF` 显示电机命令状态。黄线不能直接连接 3.3V 或 12V，必须通过 4.7k 电阻上拉到 3.3V。

电机只在PTC实际加热时旋转。开启烘干输出时先启动电机再开启PTC；温湿度达到目标、只吹风、启动等待、任务结束、长按停止或任一故障锁存时，先关闭PTC，再将PA0和PA2恢复为低电平。默认占空比由 `kMotorDutyPercent` 设置。

如果 12V 风扇侧和 STM32 侧必须保持隔离，黄线不能直接接 PA8。可用 PC817：`12V+ -> 2.2k -> 光耦LED -> 风扇黄线`；光耦晶体管侧 `集电极 -> PA8`、`发射极 -> STM32 GND`，并将 PA8 用 4.7k 上拉到 3.3V。该接法仍保持低电平为 RUN、高电平为 STOP。

Flash 驱动按 3.3V Winbond W25Qxx、SPI 模式 0 实现，启动时读取 JEDEC ID。温湿度目标使用最后两个 4KB 扇区循环保存并带 CRC32 校验，其他 Flash 空间保持未使用，可继续扩展运行数据记录。

温湿度采样按 1 秒周期触发。每次测量完成后，STM32 使用 SPI2 向 ESP32-S 发送固定 64 字节二进制状态帧；传感器离线时置无效标志，后续字段仍携带运行状态、实际输出、目标值、故障和命令确认。帧尾使用 CRC16-CCITT 校验；约 20 ms 后 STM32 读取固定 64 字节下行命令槽。通信只使用 PB13/PB14/PB15 和 GND，不占用 PB12 编码器输入。ESP32-S 未上电或未接好时，每秒只增加一次约 24 ms 的有界同步探测，不会让本地温控循环长期停住。

现有 4 针 ESP32 数据接口包含 PB13/SCK、PB14/CS、PB15/SDIO 和 GND。PB15 与 GPIO23 是唯一数据线，双方通过固定时隙避免同时驱动；PA9、PA10 和 GPIO17 均保持未连接。CRC16、会话号、命令序号、风扇/传感器/过温联锁和 5 秒运行租约全部保留，减少接线不降低这些安全检查。

## 12V 功率部分

- 12V 电源正负极接 XY-MOS 电源输入端，PTC 接 XY-MOS 负载输出端，具体端子顺序以模块丝印为准。
- 12V 100W PTC 的额定电流约为 8.33A。加入冷态电流、风扇、电机堵转和系统余量后，整机推荐使用 12V/17A 级电源。
- PTC 支路可从 15A 保险丝开始试制，但最终规格必须按实测冷态启动电流、线束和端子额定值确定。
- PTC、风扇和电机的电流不能经过 STM32 开发板供电线。
- 12V 电源负极作为星形接地点，XY-MOS、风扇、电机驱动和控制板分别回到该点，不能串接地线。
- STM32 应由 12V 经稳压模块转换为 5V 或 3.3V 供电。
- 所有控制信号必须共地；功率线应使用足够线径，并远离温湿度传感器和信号线。
- AHT20+BMP280 应安装在循环风道中、避开 PTC 直接热辐射，否则测得温度不能代表烘干箱内空气温度。
